

dependencies {
  compile(project(':sqlAnalyze-api'))
  compile(project(':sqlAnalyze-rest'))
  compile (group: 'org.quartz-scheduler', name: 'quartz', version: '2.3.1'){
    exclude(module: 'slf4j-api')
  }
}
